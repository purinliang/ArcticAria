{
	"name": "auth",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.68",
		"vitest": "~3.2.4",
		"wrangler": "^4.33.1"
	},
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"jose": "^6.1.0",
		"pino": "^9.9.0"
	}
}
